#  God rays effect

Cast rays of light from the top of the screen. **This won't work well if shown on top of the background color of the scene**. Be sure to use an object acting as a background or a floor.

![](godray-effect.gif)

The *Angle* tilts the direction of the rays. The *Center X* and *Center Y* set the origin point the rays come from: disable **Parallel** to make the rays spread out from that point (like a sun or spotlight) instead of running parallel to each other.

*Animation Speed* makes the rays shimmer over time — set it to `0` to freeze them into a static beam of light.


## Reference

All effects are listed in [the effects reference page](/gdevelop5/all-features/effects/reference/).